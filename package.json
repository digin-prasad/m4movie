{
    "name": "m4movie-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "npm install --prefix bot && npm install --prefix frontend",
        "build": "cd frontend && npm run build",
        "dev": "node start-all.js --dev",
        "start": "node start-all.js"
    }
}